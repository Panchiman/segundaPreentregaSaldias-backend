import CartManager from "/daos/mongodb/classes/cartManager.class.js"

const cartManager = new CartManager();

function addToCartButton (idCart, idProduct){
    console.log("intentamo")
    //cartManager.addToCart(idCart, idProduct);
};
